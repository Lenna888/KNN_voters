# .env.example

# =========================================================
# CONFIGURACIÓN DE FASTAPI / UVICORN
# =========================================================
# NOTA: En producción con Docker, el host y el puerto se configuran 
# en docker-compose, pero la aplicación lee estas variables por defecto.
# PORT=8000
# HOST=0.0.0.0

# =========================================================
# CONFIGURACIÓN DE BASE DE DATOS POSTGRESQL
# El formato es: postgresql://USER:PASSWORD@HOST:PORT/NAME
# =========================================================

# NOTA IMPORTANTE: En el entorno Docker Compose, 
# 'db' es el HOST, ya que es el nombre del servicio de PostgreSQL.
DATABASE_URL=postgresql://user:password@db:5432/knn_db

# =========================================================
# CONFIGURACIÓN DE ARTEFACTOS DE MACHINE LEARNING
# =========================================================

# Ruta relativa del modelo principal (Pipeline KNN) dentro del contenedor.
MODEL_PATH=app/ml/assets/knn_model.joblib

# Los archivos .joblib, .json y otros artefactos NO deben listarse aquí.
# Deben estar ubicados en la carpeta 'app/ml/assets/'